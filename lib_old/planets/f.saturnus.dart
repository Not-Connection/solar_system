part of '_index.dart';

class Saturnus extends StatelessWidget {
  const Saturnus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Saturnus'),
      ),
      body: Container(),
    );
  }
}
