part of '_index.dart';

class Merkurius extends StatelessWidget {
  const Merkurius({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Merkurius'),
      ),
      body: Container(),
    );
  }
}
