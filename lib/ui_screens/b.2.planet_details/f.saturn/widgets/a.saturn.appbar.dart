part of '../_index.dart';

class SaturnAppbar extends StatelessWidget {
  const SaturnAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
        // title: OnReactive(() => Text(_dt.rxTitle.st)),
        );
  }
}
