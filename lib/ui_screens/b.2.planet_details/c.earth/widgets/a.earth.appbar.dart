part of '../_index.dart';

class EarthAppbar extends StatelessWidget {
  const EarthAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
        // title: OnReactive(() => Text(_dt.rxTitle.st)),
        );
  }
}
