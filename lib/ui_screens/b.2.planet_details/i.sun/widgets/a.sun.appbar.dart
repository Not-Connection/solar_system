part of '../_index.dart';

class SunAppbar extends StatelessWidget {
  const SunAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
        // title: OnReactive(
        //   () => Text(
        //     textScaler: const TextScaler.linear(1.5),
        //     style: GoogleFonts.itim(),
        //     _dt.rxTitle.st,
        //   ),
        // ),
        );
  }
}
